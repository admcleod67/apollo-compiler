# Handoff documents

Optional VM / runtime improvement notes authored alongside Apollo codegen. Copy the
markdown into the VM repository when convenient; keep a single canonical copy there to
avoid drift.

| File | Purpose |
|------|---------|
| `apollo-consumer-notes.md` | General VM evolution ideas suggested by the Apollo Pascal front-end — not language-milestone blockers |
