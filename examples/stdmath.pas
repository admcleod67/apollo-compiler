program StdMath;
{ Milestone 8 Stage 1b — Wirth transcendentals via Gemini math module. }
var
  r: real;
begin
  r := sqrt(9.0);
  writeln(r);
  r := sin(0.0);
  writeln(r);
  r := cos(0.0);
  writeln(r);
  r := arctan(0.0);
  writeln(r);
  r := ln(1.0);
  writeln(r);
  r := exp(0.0);
  writeln(r);
  r := sqrt(4);
  writeln(r);
end.
